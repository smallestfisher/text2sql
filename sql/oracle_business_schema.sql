-- Oracle business schema for the Text2SQL demo data model.
SET DEFINE OFF
WHENEVER SQLERROR EXIT SQL.SQLCODE

PROMPT Creating daily_PLAN
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE daily_PLAN (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    PLAN_date VARCHAR2(16 CHAR),
    factory_code VARCHAR2(32 CHAR),
    product_ID VARCHAR2(64 CHAR),
    target_IN_glass_qty NUMBER(10),
    target_in_panel_qty NUMBER(10),
    target_Out_glass_qty NUMBER(10),
    target_Out_panel_qty NUMBER(10),
    target_Out_TTL_panel_qty NUMBER(10),
    CONSTRAINT PK_DAILY_PLAN PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating daily_inventory
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE daily_inventory (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    report_date VARCHAR2(16 CHAR),
    factory_code VARCHAR2(32 CHAR),
    ERP_FACTORY VARCHAR2(32 CHAR),
    ERP_LOCATION VARCHAR2(32 CHAR),
    product_ID VARCHAR2(64 CHAR),
    PRODUCTION_TYPE VARCHAR2(32 CHAR),
    GRADE VARCHAR2(16 CHAR),
    CHECKINCODE VARCHAR2(32 CHAR),
    TTL_Qty NUMBER(10),
    HOLD_Qty NUMBER(10),
    CONSTRAINT PK_DAILY_INVENTORY PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating monthly_plan_approved
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE monthly_plan_approved (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    plan_month VARCHAR2(10 CHAR),
    PLAN_date VARCHAR2(16 CHAR),
    factory_code VARCHAR2(32 CHAR),
    product_ID VARCHAR2(64 CHAR),
    target_IN_glass_qty NUMBER(10),
    target_in_panel_qty NUMBER(10),
    target_Out_glass_qty NUMBER(10),
    target_Out_panel_qty NUMBER(10),
    target_Out_TTL_panel_qty NUMBER(10),
    CONSTRAINT PK_MONTHLY_PLAN_APPROVED PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating oms_inventory
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE oms_inventory (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    report_month VARCHAR2(10 CHAR),
    product_ID VARCHAR2(64 CHAR),
    SBU_DESC VARCHAR2(32 CHAR),
    BU_DESC VARCHAR2(32 CHAR),
    CUSTOMER VARCHAR2(32 CHAR),
    ERP_FACTORY VARCHAR2(32 CHAR),
    ERP_LOCATION VARCHAR2(32 CHAR),
    LGORT_DL VARCHAR2(32 CHAR),
    LGORT_LX VARCHAR2(32 CHAR),
    GRADE_FL VARCHAR2(32 CHAR),
    GRADE VARCHAR2(16 CHAR),
    glass_qty NUMBER(10),
    panel_qty NUMBER(10),
    ONE_AGE_panel_qty NUMBER(10),
    TWO_AGE_panel_qty NUMBER(10),
    THREE_AGE_panel_qty NUMBER(10),
    FOUR_AGE_panel_qty NUMBER(10),
    FIVE_AGE_panel_qty NUMBER(10),
    SIX_AGE_panel_qty NUMBER(10),
    SEVEN_AGE_panel_qty NUMBER(10),
    EUGHT_AGE_panel_qty NUMBER(10),
    CONSTRAINT PK_OMS_INVENTORY PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating p_demand
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE p_demand (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    PM_VERSION VARCHAR2(32 CHAR),
    FGCODE VARCHAR2(64 CHAR),
    SBU_DESC VARCHAR2(32 CHAR),
    BU_DESC VARCHAR2(32 CHAR),
    CUSTOMER VARCHAR2(32 CHAR),
    MONTH VARCHAR2(10 CHAR),
    REQUIREMENT_QTY NUMBER(10),
    NEXT_REQUIREMENT NUMBER(10),
    LAST_REQUIREMENT NUMBER(10),
    MONTH4 NUMBER(10),
    MONTH5 NUMBER(10),
    MONTH6 NUMBER(10),
    MONTH7 NUMBER(10),
    CONSTRAINT PK_P_DEMAND PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating product_attributes
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE product_attributes (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    product_ID VARCHAR2(64 CHAR),
    application VARCHAR2(32 CHAR),
    CUT_NUM NUMBER(10),
    common_categories VARCHAR2(64 CHAR),
    IS_OXIDE VARCHAR2(1 CHAR),
    IS_XPS VARCHAR2(1 CHAR),
    IS_sloc VARCHAR2(1 CHAR),
    IS_cOATER VARCHAR2(1 CHAR),
    IS_OA VARCHAR2(1 CHAR),
    IS_Notch VARCHAR2(1 CHAR),
    CONSTRAINT PK_PRODUCT_ATTRIBUTES PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating product_mapping
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE product_mapping (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    FGCODE VARCHAR2(64 CHAR),
    cell_no VARCHAR2(64 CHAR),
    array_no VARCHAR2(64 CHAR),
    cf_no VARCHAR2(64 CHAR),
    CONSTRAINT PK_PRODUCT_MAPPING PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating production_actuals
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE production_actuals (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    work_date VARCHAR2(16 CHAR),
    FACTORY VARCHAR2(32 CHAR),
    product_ID VARCHAR2(64 CHAR),
    act_type VARCHAR2(32 CHAR),
    GLS_qty NUMBER(10),
    Panel_qty NUMBER(10),
    defect_qty NUMBER(10),
    CONSTRAINT PK_PRODUCTION_ACTUALS PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating sales_financial_perf
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE sales_financial_perf (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    report_month VARCHAR2(10 CHAR),
    SBU_DESC VARCHAR2(32 CHAR),
    BU_DESC VARCHAR2(32 CHAR),
    CUSTOMER VARCHAR2(32 CHAR),
    FGCODE VARCHAR2(64 CHAR),
    sales_qty NUMBER(10),
    FINANCIAL_qty NUMBER(10),
    CONSTRAINT PK_SALES_FINANCIAL_PERF PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating v_demand
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE v_demand (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    PM_VERSION VARCHAR2(32 CHAR),
    FGCODE VARCHAR2(64 CHAR),
    SBU_DESC VARCHAR2(32 CHAR),
    CUSTOMER VARCHAR2(32 CHAR),
    MONTH VARCHAR2(10 CHAR),
    REQUIREMENT_QTY NUMBER(10),
    NEXT_REQUIREMENT NUMBER(10),
    LAST_REQUIREMENT NUMBER(10),
    MONTH4 NUMBER(10),
    MONTH5 NUMBER(10),
    MONTH6 NUMBER(10),
    MONTH7 NUMBER(10),
    CONSTRAINT PK_V_DEMAND PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Creating weekly_rolling_plan
BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE weekly_rolling_plan (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    PM_VERSION VARCHAR2(32 CHAR),
    plan_date VARCHAR2(16 CHAR),
    factory VARCHAR2(32 CHAR),
    product_ID VARCHAR2(64 CHAR),
    plan_qty NUMBER(10),
    CONSTRAINT PK_WEEKLY_ROLLING_PLAN PRIMARY KEY (id)
)';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE = -955 THEN
      NULL;
    ELSE
      RAISE;
    END IF;
END;
/

PROMPT Done.
EXIT
